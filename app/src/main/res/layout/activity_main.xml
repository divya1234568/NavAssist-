<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="#0A0F1E">

    <!-- Topbar -->
    <LinearLayout android:layout_width="match_parent" android:layout_height="64dp"
        android:orientation="horizontal" android:gravity="center_vertical"
        android:paddingStart="16dp" android:paddingEnd="16dp" android:background="#141929">
        <TextView android:layout_width="0dp" android:layout_height="wrap_content" android:layout_weight="1"
            android:text="🧭 NavAssist" android:textSize="20sp" android:textStyle="bold" android:textColor="#FFFFFF"/>
        <TextView android:id="@+id/badge"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="BLIND MODE" android:textSize="11sp" android:textStyle="bold"
            android:textColor="#FFFFFF" android:background="#0057FF"
            android:paddingStart="12dp" android:paddingEnd="12dp"
            android:paddingTop="4dp" android:paddingBottom="4dp"/>
    </LinearLayout>

    <!-- Always-On Voice Status Bar (visible in blind mode) -->
    <TextView android:id="@+id/tv_voice_status"
        android:layout_width="match_parent" android:layout_height="36dp"
        android:text="🎤 Voice: OFF — Tap button below to enable"
        android:textSize="12sp" android:textColor="#8B97C4"
        android:gravity="center" android:background="#0D1220"/>

    <ScrollView android:layout_width="match_parent" android:layout_height="match_parent" android:padding="16dp">
        <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content" android:orientation="vertical">

            <!-- SOS - always on top, red, largest -->
            <Button android:id="@+id/btn_sos"
                android:layout_width="match_parent" android:layout_height="80dp"
                android:text="🆘  SOS EMERGENCY" android:textSize="24sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#FF3C5F" android:layout_marginBottom="16dp"/>

            <!-- Always-On Voice (Blind Mode Only) -->
            <Button android:id="@+id/btn_always_on"
                android:layout_width="match_parent" android:layout_height="64dp"
                android:text="🎙 ALWAYS-ON VOICE: OFF"
                android:textSize="15sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#1E2540"
                android:layout_marginBottom="12dp"
                android:visibility="gone"/>

            <!-- Camera Scanner -->
            <Button android:id="@+id/btn_camera"
                android:layout_width="match_parent" android:layout_height="120dp"
                android:text="📸  SCAN SURROUNDINGS&#10;AI identifies objects &amp; reads text"
                android:textSize="18sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#0057FF" android:layout_marginBottom="12dp"/>

            <!-- Google Maps Navigation -->
            <Button android:id="@+id/btn_nav"
                android:layout_width="match_parent" android:layout_height="80dp"
                android:text="🗺️  GOOGLE MAPS NAVIGATION&#10;Tap to open turn-by-turn directions"
                android:textSize="16sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#00A86B" android:layout_marginBottom="12dp"/>

            <!-- Manual Voice Command -->
            <Button android:id="@+id/btn_voice"
                android:layout_width="match_parent" android:layout_height="72dp"
                android:text="🎤  VOICE COMMAND (Manual)" android:textSize="16sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#1E2540" android:layout_marginBottom="12dp"/>

            <!-- Guardian -->
            <Button android:id="@+id/btn_guardian"
                android:layout_width="match_parent" android:layout_height="72dp"
                android:text="🛡️  GUARDIAN HUB" android:textSize="16sp" android:textStyle="bold"
                android:textColor="#FFFFFF" android:backgroundTint="#2A0F1E" android:layout_marginBottom="24dp"/>

        </LinearLayout>
    </ScrollView>
</LinearLayout>
